{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch server",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/",
            "args": ["--config", "${workspaceFolder}/configs/ksmapi.yaml", "server", "--log-level=1"],
            "cwd": "${workspaceFolder}",
            "env": {

            },
            "envFile": "${workspaceFolder}/.env",
        }
    ]
}